so in normal html page we cant send data by rest controller and return a webpage on same end point ''''
(rest : /api/hpome , controller: /api/home) ... but we can send data to controller webpage from rest controller 
using controller method parameter where we send data;